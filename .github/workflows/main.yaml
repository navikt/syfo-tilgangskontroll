name: main

on: push

jobs:
  build-and-deploy:
    uses: navikt/isworkflows/.github/workflows/kotlin-build-deploy.yml@master
    with:
      dev-cluster: dev-gcp
      prod-cluster: prod-gcp
      distroless: false
    secrets: inherit
